# Learning Arena

Local VS Code commands for independent practice in python-foundations-lab. GPT web remains the tutor. GitHub transfers the learner-requested review copy and the tutor's next prompt. The optional local browser bridge sends the review request. No telemetry or direct model/API calls.
